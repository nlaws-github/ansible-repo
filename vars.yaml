#This is where our Ansible variables are going to go, so that we can SSH into the host in our inventory.
---
ansible_port: 22 #SSH port
ansible_user: ec2-user #EC2 username 
ansible_ssh_private_key_file: ./ansible_key.pem #path to my private key on this linux machine in the cloud 